<Window x:Class="RaccoonWarehouse.Settings.LanguageSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Language Settings"
        Height="360"
        Width="520"
        WindowStartupLocation="CenterScreen"
        FlowDirection="{DynamicResource CurrentFlowDirection}"
        Background="{StaticResource AppPageBackgroundBrush}">
    <Grid Margin="24">
        <Border Style="{StaticResource AppCardBorderStyle}" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TitleTextBlock"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource AppTextBrush}"/>

                <TextBlock x:Name="DescriptionTextBlock"
                           Grid.Row="1"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource AppMutedTextBrush}"/>

                <StackPanel Grid.Row="2" Margin="0,24,0,0">
                    <TextBlock x:Name="LanguageLabelTextBlock"
                               Style="{StaticResource AppFieldLabelStyle}"/>
                    <ComboBox x:Name="LanguageComboBox"
                              Style="{StaticResource AppComboBoxStyle}"
                              SelectionChanged="LanguageComboBox_SelectionChanged"/>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Margin="0,24,0,0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="CancelButton"
                            Width="120"
                            Margin="0,0,12,0"
                            Style="{StaticResource AppSecondaryButtonStyle}"
                            Click="CancelButton_Click"/>
                    <Button x:Name="SaveButton"
                            Width="160"
                            Style="{StaticResource AppHeroButtonStyle}"
                            Click="SaveButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
