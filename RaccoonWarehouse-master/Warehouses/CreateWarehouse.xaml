<Window x:Class="RaccoonWarehouse.Warehouses.CreateWarehouse"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="إضافة مستودع"
        Height="800"
        Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        Background="{StaticResource AppPageBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="240"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" CornerRadius="0,0,36,36" Background="{StaticResource AppGreenHeroBrush}">
            <Grid Margin="34,24,34,30">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="إضافة مستودع" FontSize="34" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="28,-70,28,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource AppCardBorderStyle}" Padding="28" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="اسم المستودع" Style="{StaticResource AppFieldLabelStyle}"/>
                    <TextBox x:Name="WarehouseName" Style="{StaticResource AppTextInputStyle}" Margin="0,0,0,18"/>
                    <TextBlock Text="موقع المستودع" Style="{StaticResource AppFieldLabelStyle}"/>
                    <TextBox x:Name="WarehouseLocation" Style="{StaticResource AppTextInputStyle}" Margin="0,0,0,18"/>
                    <TextBlock Text="رقم الهاتف" Style="{StaticResource AppFieldLabelStyle}"/>
                    <TextBox x:Name="WarehousePhone" Style="{StaticResource AppTextInputStyle}" Margin="0,0,0,18"/>
                    <TextBlock Text="الوصف" Style="{StaticResource AppFieldLabelStyle}"/>
                    <TextBox x:Name="WarehouseDescription" Style="{StaticResource AppTextInputStyle}" Height="120" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,18"/>
                    <TextBlock Text="الحالة" Style="{StaticResource AppFieldLabelStyle}"/>
                    <ComboBox x:Name="WarehouseStatus" Style="{StaticResource AppComboBoxStyle}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource AppCardBorderStyle}" Padding="24">
                <StackPanel>
                    <TextBlock Text="الإجراءات" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AppPrimaryBrush}"/>
                    <TextBlock Margin="0,12,0,0" FontSize="14" Foreground="{StaticResource AppMutedTextBrush}" Text="احفظ المستودع الجديد أو ارجع إلى جدول المستودعات."/>
                    <Button x:Name="CreateWarehouseBtn" Margin="0,34,0,0" Width="200" Style="{StaticResource AppFormButtonStyle}" Click="CreateWarehouseBtn_Click">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <fa:ImageAwesome Icon="Plus" Width="16" Height="16" Margin="0,0,8,0" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="إضافة"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BackBtn" Margin="0,14,0,0" Width="200" Style="{StaticResource AppSecondaryButtonStyle}" Click="BackBtn_Click">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <fa:ImageAwesome Icon="ArrowRight" Width="16" Height="16" Margin="0,0,8,0" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            <TextBlock Text="رجوع"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
