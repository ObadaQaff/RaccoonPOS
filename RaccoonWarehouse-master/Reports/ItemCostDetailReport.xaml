<Window x:Class="RaccoonWarehouse.Reports.ItemCostDetailReport"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="تفاصيل تكلفة الأصناف"
        Height="860"
        Width="1500"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft">
    <Grid Background="#EEF2F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Margin="20" Padding="26" CornerRadius="26" Background="#2F7CC1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="تفاصيل تكلفة الأصناف" Foreground="White" FontSize="34" FontWeight="Bold"/>
                    <TextBlock Text="كمية الصنف الحالية وتكلفته وقيمته الإجمالية" Foreground="#D9E9F8" FontSize="16" Margin="0,8,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="#244D74" CornerRadius="18" Padding="18,10">
                    <StackPanel>
                        <TextBlock Text="إجمالي القيمة" Foreground="#BFD9EF" FontSize="13" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TotalValueText" Text="0.000" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="White" CornerRadius="22" Padding="18" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="بحث" FontWeight="SemiBold" Foreground="#34506B"/>
                        <TextBox x:Name="SearchTextBox" Height="38" Margin="0,8,0,0" Padding="10,6"/>
                    </StackPanel>

                    <Border Grid.Column="1" Margin="12,0,0,0" Background="#F4F8FB" CornerRadius="16" Padding="12">
                        <StackPanel>
                            <TextBlock Text="عدد السطور" Foreground="#6B8094" FontSize="13" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TotalItemsText" Text="0" Foreground="#1F4363" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                        <Button x:Name="GenerateReportBtn" Width="170" Height="42" Margin="0,0,12,0" Click="GenerateReportBtn_Click" Background="#2F7CC1" Foreground="White" BorderBrush="Transparent" Cursor="Hand">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <fa:ImageAwesome Icon="Search" Width="16" Height="16" Foreground="White"/>
                                <TextBlock Text="عرض التقرير" Margin="8,0,0,0" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BackBtn" Width="140" Height="42" Click="BackBtn_Click" Background="#E9EEF3" Foreground="#2B4A67" BorderBrush="Transparent" Cursor="Hand">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <fa:ImageAwesome Icon="ArrowLeft" Width="16" Height="16" Foreground="#2B4A67"/>
                                <TextBlock Text="رجوع" Margin="8,0,0,0" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="White" CornerRadius="22" Padding="14">
                <DataGrid x:Name="ItemCostDetailGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowBackground="#FFFFFF"
                          AlternatingRowBackground="#F6F9FC"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="كود الصنف" Binding="{Binding ItemID}" Width="110"/>
                        <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ItemName}" Width="2*"/>
                        <DataGridTextColumn Header="الوحدة" Binding="{Binding UnitName}" Width="*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, StringFormat=N3}" Width="*"/>
                        <DataGridTextColumn Header="التكلفة" Binding="{Binding Cost, StringFormat=N3}" Width="*"/>
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total, StringFormat=N3}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Margin="20,0,20,20" Background="#E7DCC8" CornerRadius="18" Padding="12">
            <TextBlock Text="Raccoon Warehouse 2025 ©" HorizontalAlignment="Center" Foreground="#365A7C" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>
